# Repositories

Concrete data-access implementations (e.g. an EF Core-backed repository) of the repository interfaces declared in TimeClock.Domain/Interfaces, kept in Infrastructure so Domain never depends on a specific storage technology.
