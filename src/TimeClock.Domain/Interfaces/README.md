# Interfaces

Contracts such as IClockEntryRepository and IEmployeeService that describe what a collaborator must do, not how it does it, so consumers can depend on behavior instead of a concrete implementation.
