# Constants

Fixed values (business rule thresholds, format strings, default limits) that must never vary at runtime. Naming them once here means they are not duplicated as magic numbers or strings scattered across the codebase.
