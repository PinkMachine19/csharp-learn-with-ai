<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
  <ItemGroup>
    <!-- Mirrors the real Parm.Practice.WebApi.csproj: references only the domain project.
         EF Core (TimeClock.Infrastructure) is NOT referenced here, matching the real WebApi,
         which still uses the same in-memory ClockEntryRepository as the console app. -->
    <ProjectReference Include="..\TimeClock.Domain\TimeClock.Domain.csproj" />
  </ItemGroup>
</Project>
