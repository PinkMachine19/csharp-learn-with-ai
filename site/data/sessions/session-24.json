{
  "number": 24,
  "category": "query execution timing",
  "estimatedTime": "40 minutes",
  "connection": "Session 23 grouped and joined ClockEntry data. This session asks when a LINQ query actually runs, using GetEmployeeClockEntries's return type (IEnumerable<ClockEntry>, deferred) and Session 32-33's future EF Core queries (IQueryable<T>, deferred until a provider translates them) as the two real shapes this course uses.",
  "objectives": [
    "Explain that GetEmployeeClockEntries returns an unevaluated query, not a materialized list -- the Where clause only runs when something enumerates the result.",
    "Predict what happens if _clockEntries changes between calling GetEmployeeClockEntries and enumerating its result.",
    "Use .ToList() to force immediate evaluation and capture a stable snapshot.",
    "Distinguish IEnumerable<T> (executes locally, in-process) from IQueryable<T> (translated by a provider, as EF Core will do starting in Session 32)."
  ],
  "visuals": [
    {
      "diagram": "pipeline",
      "title": "GetEmployeeClockEntries returns an unevaluated query",
      "explanation": "IEnumerable<ClockEntry> is a promise, not a result -- the real Where clause has not run yet when GetEmployeeClockEntries returns."
    },
    {
      "diagram": "call",
      "title": ".Sum() is what actually triggers execution",
      "explanation": "CalculateTotalHours's .Sum() call is the real point where GetEmployeeClockEntries's deferred query is finally enumerated."
    },
    {
      "diagram": "return",
      "title": ".ToList() forces a fixed snapshot",
      "explanation": "DeferredEntryQuery.Completed(entries).ToList() materializes immediately -- later changes to entries will not affect the snapshot."
    },
    {
      "diagram": "invariant",
      "title": "IQueryable defers to a provider, not just to enumeration",
      "explanation": "Session 32-33's EF Core queries return IQueryable<ClockEntryEntity> -- the same deferred principle, translated to SQL instead of run in-process."
    }
  ],
  "preQuiz": [
    {
      "prompt": "When does GetEmployeeClockEntries's Where clause actually run?",
      "options": [
        "When the returned IEnumerable<ClockEntry> is enumerated (a foreach, ToList, or Sum) -- not when GetEmployeeClockEntries itself is called",
        "Immediately, inside the GetEmployeeClockEntries call",
        "Never -- Where is evaluated at compile time"
      ],
      "correct": 0,
      "explanation": "IEnumerable's Where is deferred: calling GetEmployeeClockEntries builds a query description, and CalculateTotalHours's .Sum() is what actually walks _clockEntries and applies the filter."
    },
    {
      "prompt": "If a new ClockEntry is added to _clockEntries after GetEmployeeClockEntries returns but before .Sum() enumerates it, what happens?",
      "options": [
        "The new entry can be included in the sum, since the query re-reads the live list at enumeration time",
        "The new entry is guaranteed to be excluded",
        "An exception is thrown"
      ],
      "correct": 0,
      "explanation": "Deferred execution means the query is a live view over _clockEntries at the moment it is enumerated, not a snapshot taken when GetEmployeeClockEntries was called."
    },
    {
      "prompt": "What does .ToList() do differently from just holding an IEnumerable<ClockEntry> reference?",
      "options": [
        "It forces immediate enumeration and copies matching elements into a new List, fixed at that moment",
        "It changes the Where clause's filtering logic",
        "It converts the query into an IQueryable"
      ],
      "correct": 0,
      "explanation": "ToList materializes: it runs the deferred query right now and returns a stable snapshot that later changes to the source will not affect."
    }
  ],
  "concept": [
    {
      "title": "GetEmployeeClockEntries returns a query, not a list",
      "paragraphs": [
        "The real method's return type is IEnumerable<ClockEntry> -- a promise of a sequence, not the sequence itself. The .Where() call inside it builds that promise but does not walk _clockEntries yet.",
        "CalculateTotalHours is what actually triggers execution, because .Sum() has to enumerate every element to add it up."
      ],
      "code": "// Repositories/ClockEntryRepository.cs -- returns an unevaluated query\npublic IEnumerable<ClockEntry> GetEmployeeClockEntries(int employeeId, DateTime startDate, DateTime endDate)\n{\n    return _clockEntries.Where(entry => /* ...three conditions... */ true);\n}\n\n// Services/PayrollService.cs -- .Sum() is what forces evaluation\nreturn clockEntries.Sum(x => x.GetDuartion().TotalHours);"
    },
    {
      "title": "ToList forces evaluation now",
      "paragraphs": [
        "DeferredEntryQuery.Completed returns an IEnumerable<ClockEntry> that has not run yet. Calling .ToList() on it walks _clockEntries immediately and returns a fixed List<ClockEntry> snapshot.",
        "The difference is observable: if the source list changes after the deferred query was built but before it is enumerated, an un-materialized IEnumerable can see the change; a .ToList() snapshot cannot."
      ],
      "code": "public static IEnumerable<ClockEntry> Completed(IEnumerable<ClockEntry> entries) =>\n    entries.Where(entry => entry.ClockOut is not null);\n\n// Caller decides when (or whether) to materialize:\nList<ClockEntry> snapshot = DeferredEntryQuery.Completed(entries).ToList();"
    },
    {
      "title": "IQueryable defers differently: to a provider, not just to enumeration",
      "paragraphs": [
        "GetEmployeeClockEntries's IEnumerable<ClockEntry> runs entirely in this process, in plain C#. Session 32-33's EF Core queries return IQueryable<ClockEntryEntity> instead: LINQ builds an expression tree that a database provider translates into SQL only when enumerated.",
        "Same deferred-until-enumerated principle, different execution target -- in-process C# versus a translated database query."
      ],
      "code": "dotnet test TimeClock.sln --configuration Release"
    }
  ],
  "lab": {
    "objective": "Trace GetEmployeeClockEntries's deferred IEnumerable<ClockEntry> through CalculateTotalHours's .Sum() call, and confirm .ToList() produces a genuinely independent snapshot.",
    "startingCondition": "Sessions 02–22 are complete and all existing tests pass.",
    "steps": [
      "Predict the successful path and one boundary or failure case.",
      "Add src/TimeClock.Domain/DeferredEntryQuery.cs with a narrow public contract.",
      "Add tests/TimeClock.Domain.Tests/DeferredEntryQueryTests.cs for the prediction and boundary case.",
      "Run the focused tests and inspect the observed values or control flow.",
      "Run the full Release build and test suite."
    ],
    "expectedBehavior": "The new tests establish that IEnumerable queries normally execute when enumerated, ToList materializes a snapshot, and IQueryable records an expression for a provider; prior behavior remains unchanged.",
    "validation": "dotnet build TimeClock.sln --configuration Release && dotnet test TimeClock.sln --configuration Release"
  },
  "expectedFiles": [
    {
      "path": "src/TimeClock.Domain/DeferredEntryQuery.cs",
      "action": "Add",
      "why": "Provide the DeferredEntryQuery.Completed example demonstrating IEnumerable<ClockEntry> deferred execution."
    },
    {
      "path": "tests/TimeClock.Domain.Tests/DeferredEntryQueryTests.cs",
      "action": "Add",
      "why": "Verify the contract and its boundary case."
    },
    {
      "path": "site/data/sessions/session-23.json",
      "action": "Add",
      "why": "Publish synchronized lesson, lab, and quiz material."
    }
  ],
  "commit": "session-23: teach deferred execution and materialization",
  "review": [
    "The public API states one coherent responsibility.",
    "Dependencies and state changes are explicit.",
    "The example remains deterministic and compact.",
    "Tests cover success and a meaningful boundary.",
    "No nullable warnings are introduced.",
    "The complete suite passes."
  ],
  "postQuiz": [
    {
      "prompt": "Why does CalculateTotalHours never need to call .ToList() on the result of GetEmployeeClockEntries?",
      "options": [
        "It only enumerates the sequence once (inside .Sum()), so there is no benefit to materializing a separate snapshot first",
        "IEnumerable<ClockEntry> cannot support .ToList()",
        "GetEmployeeClockEntries already returns a List"
      ],
      "correct": 0,
      "explanation": "Materializing is worth the extra allocation when a sequence will be enumerated more than once, or when you need a stable snapshot -- CalculateTotalHours does neither."
    },
    {
      "prompt": "What is the risk of enumerating the same un-materialized deferred query twice in a row (e.g. once to count, once to sum)?",
      "options": [
        "The underlying Where/filter logic re-runs both times, which is wasted work and, for a database-backed IQueryable, a second round trip",
        "The second enumeration always throws an exception",
        "There is no risk; deferred queries are cached automatically after the first enumeration"
      ],
      "correct": 0,
      "explanation": "Deferred execution means every enumeration re-runs the query from scratch -- exactly why Session 32-33's EF Core queries call .ToListAsync() once and reuse the materialized result."
    },
    {
      "prompt": "Once Session 32 introduces IQueryable<ClockEntryEntity>, what changes about where the Where clause actually executes?",
      "options": [
        "The database provider translates the Where clause into SQL and executes it in the database, not in this process",
        "Nothing changes -- IQueryable behaves identically to IEnumerable",
        "The Where clause runs at compile time instead of runtime"
      ],
      "correct": 0,
      "explanation": "This is the concrete difference Session 33 explores: IEnumerable filters in-process C# objects; IQueryable's expression tree is translated and executed by the database provider."
    }
  ],
  "reflections": [
    "Why does GetEmployeeClockEntries's deferred IEnumerable<ClockEntry> behave differently from a materialized List<ClockEntry> if _clockEntries changes between the call and the enumeration?",
    "When would you deliberately call .ToList() on GetEmployeeClockEntries's result, even though CalculateTotalHours does not?",
    "What concrete difference will Session 33 show between this session's in-process IEnumerable queries and EF Core's IQueryable queries?"
  ],
  "whatBreaks": "Calling .ToList() unnecessarily inside GetEmployeeClockEntries would force evaluation before the caller decides what to do with the result, silently changing its behavior from a live view to a fixed snapshot. Enumerating a deferred query multiple times when the underlying provider is a database (Session 33) means multiple round trips, not one. Confusing IEnumerable's in-process execution with IQueryable's provider-translated execution leads to assuming SQL-level operations (like case-insensitive collation) that only IQueryable's translation actually provides.",
  "summary": "GetEmployeeClockEntries returns an unevaluated IEnumerable<ClockEntry>; CalculateTotalHours's .Sum() is what actually triggers the Where clause. .ToList() forces immediate evaluation into a fixed snapshot. Session 32-33's EF Core IQueryable<T> defers the same way, but to a database provider instead of in-process enumeration."
}
