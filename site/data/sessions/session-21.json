{
  "number": 21,
  "category": "query pipelines",
  "estimatedTime": "40 minutes",
  "connection": "Session 20 created PayrollService with a clear foreach loop over completed entries. This session refactors that known behavior into LINQ and adds one small ordered-history query; it does not assume additional services or tests already exist.",
  "objectives": [
    "Read a LINQ pipeline from source to result.",
    "Use Where to keep completed entries and OrderByDescending to show newest entries first.",
    "Use Sum with an inline projection to calculate total hours.",
    "Create PayrollService with the interface dependency established in Session 20."
  ],
  "visuals": [
    {
      "diagram": "pipeline",
      "title": "A query is a sequence of transformations",
      "explanation": "Source entries flow through filtering and ordering; each operator returns another sequence."
    },
    {
      "diagram": "branch",
      "title": "Where removes open entries",
      "explanation": "Only entries with a clock-out can safely provide a completed duration."
    },
    {
      "diagram": "call",
      "title": "Sum projects and aggregates",
      "explanation": "The lambda converts each ClockEntry to hours; Sum combines those numbers into one total."
    },
    {
      "diagram": "order",
      "title": "Ordering states a visible requirement",
      "explanation": "OrderByDescending on ClockIn produces newest-first history rather than relying on source insertion order."
    }
  ],
  "preQuiz": [
    {
      "prompt": "Why filter open entries before calling GetDuration?",
      "options": [
        "An open entry has no completed duration",
        "Where changes the repository",
        "Sum requires nullable values"
      ],
      "correct": 0,
      "explanation": "The filter protects the duration invariant."
    },
    {
      "prompt": "What does the lambda in Sum return?",
      "options": [
        "The numeric hours for one entry",
        "A new repository",
        "A Boolean"
      ],
      "correct": 0,
      "explanation": "Sum projects each element to a number and aggregates those numbers."
    },
    {
      "prompt": "Does OrderByDescending modify the source collection?",
      "options": [
        "No; it returns an ordered sequence",
        "Yes",
        "Only for Lists"
      ],
      "correct": 0,
      "explanation": "LINQ query operators do not reorder the original List."
    }
  ],
  "concept": [
    {
      "title": "Where filters by a requirement",
      "paragraphs": [
        "A completed-history query excludes entries whose clock-out is null before any duration calculation.",
        "Use the Clockout/Clockout spelling present in your current ClockEntry class."
      ],
      "code": "entries.Where(entry => entry.Clockout is not null)"
    },
    {
      "title": "Sum can project and aggregate",
      "paragraphs": [
        "PayrollService obtains completed entries through IClockEntryRepository, then maps each one to GetDuration().Value.TotalHours inside Sum.",
        "There is no need for a separate Select when no caller needs the intermediate sequence of numbers."
      ],
      "code": "return entries.Sum(entry => entry.GetDuration().Value.TotalHours);"
    },
    {
      "title": "Side note: why .Value appears here",
      "paragraphs": [
        "This nullable-value detail supports the calculation, but it is not a new LINQ requirement. Read it when you want to understand the expression more deeply; then return to the main Sum example.",
        "In the learner application, GetDuration returns TimeSpan?. The question mark means the result is a nullable value type: it is a small wrapper that either contains a TimeSpan or contains no value. TotalHours belongs to TimeSpan, so code must first access the TimeSpan stored inside that wrapper. The .Value member performs that unwrap.",
        "Using .Value is safe at this point because GetEmployeeClockEntries returns only completed entries, and GetDuration rejects an open entry. That earlier filtering is the proof that a duration exists; .Value communicates that this code expects the proof to hold.",
        "GetDuration()?.TotalHours ?? 0 also compiles: ?. stops when the duration is null, and ?? 0 substitutes zero. However, silently counting a missing duration as zero can hide a broken repository or domain invariant. Use that fallback only when zero is truly the required meaning of a missing duration, not merely to satisfy the compiler.",
        "If GetDuration is later redesigned to return non-nullable TimeSpan because it always returns a duration or throws, no unwrap is needed: call GetDuration().TotalHours directly."
      ],
      "code": "TimeSpan duration = entry.GetDuration().Value; // TimeSpan? -> TimeSpan\ndouble hours = duration.TotalHours;"
    },
    {
      "title": "Ordering should express the caller's need",
      "paragraphs": [
        "CompletedNewestFirst uses OrderByDescending because a history screen normally shows recent entries first.",
        "The returned IEnumerable remains a query; Session 23 examines when it actually executes."
      ],
      "code": "return entries\n    .Where(entry => entry.Clockout is not null)\n    .OrderByDescending(entry => entry.ClockIn);"
    }
  ],
  "lab": {
    "objective": "Refactor the Session 20 total-hours loop to Sum and create one query that filters completed entries and orders them newest first.",
    "startingCondition": "IClockEntryRepository and ClockEntryService exist. PayrollService and ClockEntryQueries do not exist yet.",
    "steps": [
      "Create PayrollService.cs in the Domain project's Services folder and declare a public sealed class named PayrollService.",
      "Inside PayrollService, add a private readonly IClockEntryRepository field named _clockEntryRepository.",
      "Add a public PayrollService constructor with an IClockEntryRepository parameter named clockEntryRepository, then assign it to _clockEntryRepository.",
      "Add a public CalculateTotalHours method that returns double and accepts int employeeId, DateTime startDate, and DateTime endDate. Leave its body unfinished for now.",
      "Inside CalculateTotalHours, call _clockEntryRepository.GetEmployeeClockEntries(employeeId, startDate, endDate) and store the result in IEnumerable<ClockEntry> entries.",
      "Return entries.Sum(entry => entry.GetDuration().Value.TotalHours). Here .Value unwraps the TimeSpan? so TotalHours can be read; the repository's completed-entry filter is what makes that unwrap valid.",
      "Create ClockEntryQueries.cs in the Domain project and declare a public static class named ClockEntryQueries.",
      "Add a public static CompletedNewestFirst method that returns IEnumerable<ClockEntry> and accepts IEnumerable<ClockEntry> entries.",
      "Inside CompletedNewestFirst, filter entries where Clockout is not null, order them by ClockIn descending, and return the pipeline without calling ToList.",
      "Open App/Program.cs, preserve earlier sections, and append a Session 21 section that constructs PayrollService, displays total hours, and enumerates CompletedNewestFirst.",
      {
        "text": "Run the App project, then build and test PinkMachine19.TimeClock.sln. Compare with the existing solution reveal after attempting both queries.",
        "code": "dotnet run --project src/PinkMachine19.TimeClock.App/PinkMachine19.TimeClock.App.csproj --configuration Release\ndotnet build PinkMachine19.TimeClock.sln --configuration Release && dotnet test PinkMachine19.TimeClock.sln --configuration Release --no-build",
        "reveals": [
          {
            "title": "Solution",
            "text": "Match GetDuration and Clockout/Clockout to your existing ClockEntry API.",
            "code": "public sealed class PayrollService\n{\n    private readonly IClockEntryRepository _clockEntries;\n\n    public PayrollService(IClockEntryRepository clockEntries)\n    {\n        _clockEntries = clockEntries;\n    }\n\n    public double CalculateTotalHours(int employeeId, DateTime startDate, DateTime endDate)\n    {\n        IEnumerable<ClockEntry> entries = _clockEntries.GetEmployeeClockEntries(employeeId, startDate, endDate);\n        return entries.Sum(entry => entry.GetDuration().Value.TotalHours);\n    }\n}\n\npublic static class ClockEntryQueries\n{\n    public static IEnumerable<ClockEntry> CompletedNewestFirst(IEnumerable<ClockEntry> entries) => entries\n        .Where(entry => entry.Clockout is not null)\n        .OrderByDescending(entry => entry.ClockIn);\n}"
          }
        ]
      }
    ],
    "expectedBehavior": "Production uses LINQ to express existing filtering and ordering requirements without adding a second fictional feature.",
    "validation": "dotnet build PinkMachine19.TimeClock.sln --configuration Release && dotnet test PinkMachine19.TimeClock.sln --configuration Release --no-build",
    "clickHighlightInstructions": true,
    "stepCheckboxes": true
  },
  "expectedFiles": [
    {
      "path": "src/PinkMachine19.TimeClock.Domain/Services/PayrollService.cs",
      "action": "Add",
      "why": "Create total-hours behavior when LINQ provides its implementation."
    },
    {
      "path": "src/PinkMachine19.TimeClock.Domain/ClockEntryQueries.cs",
      "action": "Add",
      "why": "Create a completed newest-first query."
    },
    {
      "path": "src/PinkMachine19.TimeClock.App/Program.cs",
      "action": "Modify",
      "why": "Observe both results."
    },
    {
      "path": "site/data/sessions/session-21.json",
      "action": "Modify",
      "why": "Align the LINQ lesson with real prerequisites."
    }
  ],
  "commit": "session-21: teach linq filtering projection and ordering",
  "review": [
    "The lab uses production because that is the most natural environment for this concept.",
    "Every referenced application artifact was created in this session or an earlier session.",
    "Earlier production code, tests, and ScratchPad experiments remain intact."
  ],
  "postQuiz": [
    {
      "prompt": "Why can Sum contain entry => entry.GetDuration().Value.TotalHours?",
      "options": [
        "Sum accepts a selector that produces the number to add",
        "GetDuration returns IEnumerable",
        "Where requires it"
      ],
      "correct": 0,
      "explanation": "Projection and aggregation can be combined when the intermediate values are not needed."
    },
    {
      "prompt": "Why is .Value reasonable after GetEmployeeClockEntries returns the entries?",
      "options": [
        "The repository returns only completed entries, so each duration is expected to exist",
        ".Value converts hours from double to int",
        ".Value creates a missing clock-out time"
      ],
      "correct": 0,
      "explanation": ".Value unwraps TimeSpan?; it does not create a value. The completed-entry filter supplies the reason the code can expect that value to exist."
    },
    {
      "prompt": "Why does CompletedNewestFirst not call ToList?",
      "options": [
        "The caller has not requested a snapshot",
        "ToList cannot follow OrderBy",
        "IEnumerable is mutable"
      ],
      "correct": 0,
      "explanation": "Session 23 will make the execution-time choice explicit."
    },
    {
      "prompt": "What changes if OrderBy is used instead of OrderByDescending?",
      "options": [
        "The result becomes oldest first",
        "The source List is reordered",
        "Open entries are included"
      ],
      "correct": 0,
      "explanation": "Only the ordering direction changes."
    }
  ],
  "reflections": [
    "Which requirement does each LINQ operator represent?",
    "When would a separate Select make the intermediate values clearer?",
    "Why is unwrapping the duration with .Value different from replacing a missing duration with zero?",
    "Why should open-entry filtering happen before duration calculation?"
  ],
  "whatBreaks": "Removing the completed-entry filter allows an open entry into code that calls GetDuration and can trigger an exception. Reversing OrderByDescending changes newest-first history to oldest-first. Adding ToList here would force materialization before Session 23 explains that choice.",
  "summary": "Session 21 refactors a known foreach total into Sum and adds one completed, newest-first query. Every operator serves a visible requirement, and the lesson uses the ClockEntry member names present in the learner's source.",
  "learningEnvironment": "production",
  "buildState": {
    "creates": [
      "PayrollService",
      "ClockEntryQueries"
    ],
    "extends": [
      "App LINQ demonstration"
    ]
  }
}
