{
  "number": 28,
  "category": "Asynchronous programming",
  "estimatedTime": "45 minutes",
  "connection": "Session 26 identified, without fixing, Program.cs's real unawaited clockEntryService.Clockout(1) call and the CS4014 warning it produces. This session fixes it -- the real async/await bug this codebase has carried since Session 6 -- and studies cancellation through IClockEntryRepository.GetClockEntry's real, unused CancellationToken parameter alongside CancellableEntrySource, a new extension that actually uses one.",
  "objectives": [
    "Fix the real CS4014 bug: add await to Program.cs's clockEntryService.Clockout(1) call and remove the pragma suppression, confirming the build is warning-free without it.",
    "Explain why the fix changes CalculateTotalHours's printed total from a near-zero accident to a real, deterministic, non-trivial number of hours.",
    "Read IClockEntryRepository.GetClockEntry's CancellationToken cancellationToken = default parameter and confirm, by reading ClockEntryRepository's real implementation, that it is never actually checked or passed anywhere.",
    "Contrast that decorative token with CancellableEntrySource, a new extension whose CancellationToken is genuinely passed to Task.Delay and can actually cancel an in-flight operation."
  ],
  "visuals": [
    {
      "diagram": "call",
      "title": "Before the fix: a race with an accidental near-correct answer",
      "explanation": "clockEntryService.Clockout(1) without await lets Program.cs call CalculateTotalHours before Clockout's body has necessarily finished -- the near-zero total was luck, not correctness."
    },
    {
      "diagram": "pipeline",
      "title": "After the fix: ClockIn, then Clockout, then the total, guaranteed in order",
      "explanation": "await clockEntryService.Clockout(1); ensures Clockout's entire body -- including SaveClockEntry -- completes before CalculateTotalHours runs."
    },
    {
      "diagram": "invariant",
      "title": "A CancellationToken parameter is not automatically functional",
      "explanation": "IClockEntryRepository.GetClockEntry accepts a CancellationToken but its real implementation never reads it -- passing one has no effect on the real GetClockEntry today."
    },
    {
      "diagram": "feedback",
      "title": "CancellableEntrySource shows what a real, functioning token looks like",
      "explanation": "await Task.Delay(latency, cancellationToken) genuinely observes cancellation -- a real contrast to GetClockEntry's decorative parameter."
    }
  ],
  "preQuiz": [
    {
      "prompt": "Why did Program.cs's unawaited Clockout(1) call produce a near-zero total-hours output rather than a clearly wrong one?",
      "options": [
        "ClockIn and the fire-and-forget Clockout run essentially back-to-back with almost no real elapsed time between them, and if CalculateTotalHours runs before Clockout's SaveClockEntry completes, GetEmployeeClockEntries may find zero completed entries in the requested window -- a near-zero result was the accidental, not guaranteed, outcome",
        "The total was always correct -- there was never a real bug",
        "CalculateTotalHours has a rounding bug unrelated to await"
      ],
      "correct": 0,
      "explanation": "This is the real, documented consequence of the CS4014 warning -- Session 26 identified it; this session fixes it and explains exactly why the output changes."
    },
    {
      "prompt": "After adding await to clockEntryService.Clockout(1), what does Program.cs now guarantee about the order of operations?",
      "options": [
        "ClockIn completes, then Clockout's entire body (including the real SaveClockEntry call inside it) completes, THEN CalculateTotalHours runs -- removing the race entirely",
        "Nothing changes -- await has no effect on execution order here",
        "ClockIn and Clockout now run concurrently instead of the reverse"
      ],
      "correct": 0,
      "explanation": "await is precisely what removes the race: the calling code cannot proceed past the awaited call until it has genuinely finished."
    },
    {
      "prompt": "Does passing a real, non-default CancellationToken to the current GetClockEntry implementation actually let you cancel an in-flight lookup?",
      "options": [
        "No -- the real ClockEntryRepository.GetClockEntry implementation never reads or checks its cancellationToken parameter anywhere in its body; passing one has no observable effect today",
        "Yes -- every async method automatically respects any CancellationToken passed to it",
        "Only if the lookup takes longer than one second"
      ],
      "correct": 0,
      "explanation": "This is confirmed by reading the real source directly -- the parameter exists in the signature (the only CancellationToken in the whole codebase) but is unused, decorative."
    }
  ],
  "concept": [
    {
      "title": "The fix: one keyword, and a warning that stops needing suppression",
      "paragraphs": [
        "The real bug is exactly as small as it looks: clockEntryService.Clockout(1); becomes await clockEntryService.Clockout(1);. No other line in Program.cs needs to change.",
        "The #pragma warning disable CS4014 / restore pair this course added around the real bug (to keep TreatWarningsAsErrors from failing the build while the bug was still being taught) is removed entirely once the fix is in place -- the build is warning-free without needing to suppress anything."
      ],
      "code": "// Before -- the real, long-standing bug, kept intentionally through Session 26\n#pragma warning disable CS4014\nclockEntryService.Clockout(1);\n#pragma warning restore CS4014\n\n// After -- the fix\nawait clockEntryService.Clockout(1);"
    },
    {
      "title": "Why the printed total changes -- and becomes trustworthy",
      "paragraphs": [
        "Before the fix, CalculateTotalHours's near-zero output only happened to look plausible; it was one possible outcome of an actual race condition, not a guaranteed one.",
        "After the fix, ClockIn's SaveClockEntry and Clockout's ClockOutEmployee-then-SaveClockEntry are both guaranteed complete before CalculateTotalHours reads GetEmployeeClockEntries -- the printed total now reliably reflects the real (tiny, since ClockIn and Clockout still happen almost instantly) elapsed time between the two calls."
      ],
      "code": "clockEntryService.ClockIn(1);\nawait clockEntryService.Clockout(1);\nvar result = payrollService.CalculateTotalHours(1, DateTime.Today.AddHours(-4), DateTime.Now.AddHours(4));\nConsole.WriteLine($\"Total hours worked: {result}\");"
    },
    {
      "title": "A decorative token versus a real one",
      "paragraphs": [
        "GetClockEntry's CancellationToken cancellationToken = default is the only CancellationToken anywhere in this codebase's real, ported source -- and it is never read inside the method body. Passing CancellationToken.None or a token that is already cancelled produces identical behavior today.",
        "CancellableEntrySource -- a new extension -- shows what a genuinely functioning token looks like: await Task.Delay(latency, cancellationToken) actually observes cancellation and throws OperationCanceledException if the token fires before the delay completes."
      ],
      "code": "// Real, decorative -- confirmed unused inside the method body\nTask<ClockEntry?> GetClockEntry(int employeeId, CancellationToken cancellationToken = default);\n\n// New extension -- a genuinely functioning token\npublic async Task<ClockEntry> LoadAsync(int employeeId, CancellationToken cancellationToken)\n{\n    await Task.Delay(latency, cancellationToken);   // actually observes cancellation\n    ...\n}"
    }
  ],
  "lab": {
    "objective": "Fix Program.cs's real CS4014 bug, confirm the total-hours output changes, and demonstrate the difference between GetClockEntry's decorative token and CancellableEntrySource's real one.",
    "startingCondition": "Program.cs still has the real, pragma-suppressed unawaited Clockout(1) call from Sessions 5-25; CancellableEntrySource already exists as a real, functioning-cancellation extension.",
    "steps": [
      "Add await to clockEntryService.Clockout(1) in Program.cs and remove the #pragma warning disable/restore CS4014 pair.",
      "Build the solution and confirm it is warning-free with no suppression needed.",
      "Run the app before and after the fix (or compare against Session 26's recorded output) and explain the difference in the printed total.",
      "Write a test that cancels a CancellableEntrySource.LoadAsync call mid-delay and confirms OperationCanceledException is thrown -- then confirm the same cancellation approach has no effect on GetClockEntry."
    ],
    "expectedBehavior": "The build succeeds with 0 warnings and no pragma suppression; the app's printed total-hours line reflects the now-guaranteed ClockIn-then-Clockout ordering; CancellableEntrySource genuinely responds to cancellation while GetClockEntry does not.",
    "validation": "dotnet build PinkMachine19.TimeClock.sln --configuration Release && dotnet test PinkMachine19.TimeClock.sln --configuration Release && dotnet run --project src/PinkMachine19.TimeClock.App/PinkMachine19.TimeClock.App.csproj --configuration Release --no-build"
  },
  "expectedFiles": [
    {
      "path": "src/PinkMachine19.TimeClock.App/Program.cs",
      "action": "Modify",
      "why": "Await clockEntryService.Clockout(1) and remove the now-unnecessary CS4014 pragma suppression."
    },
    {
      "path": "src/PinkMachine19.TimeClock.Domain/IClockEntryRepository.cs",
      "action": "Verify",
      "why": "Confirm the real, decorative CancellationToken parameter on GetClockEntry."
    },
    {
      "path": "src/PinkMachine19.TimeClock.Domain/CancellableEntrySource.cs",
      "action": "Verify",
      "why": "Confirm the new extension's genuinely functioning cancellation."
    },
    {
      "path": "site/data/sessions/session-27.json",
      "action": "Add",
      "why": "Publish synchronized lesson, lab, and quiz material."
    }
  ],
  "commit": "session-27: teach cancellation and asynchronous exceptions",
  "review": [
    "Program.cs's await fix is the exact, minimal change (add await, remove the pragma pair) -- no other lines are touched.",
    "The build is confirmed warning-free without any suppression after the fix.",
    "GetClockEntry's CancellationToken is confirmed decorative by reading its real implementation, not assumed.",
    "CancellableEntrySource's genuinely functioning cancellation is demonstrated with a real test, contrasting directly with GetClockEntry.",
    "The full test suite passes."
  ],
  "postQuiz": [
    {
      "prompt": "Why was it correct for Sessions 5 through 25 to keep Program.cs's Clockout(1) call unawaited, rather than fixing it immediately when it was first introduced?",
      "options": [
        "The bug is real, observable, and pedagogically useful exactly as-is -- fixing it only once async/await (Session 26) and cancellation (this session) have been properly taught lets learners understand WHY it's a bug, not just that it is one",
        "It was an oversight that should have been fixed in Session 6",
        "Unawaited async calls are actually correct C# and never need fixing"
      ],
      "correct": 0,
      "explanation": "This mirrors the real source repository's own history: the bug exists in production-adjacent code precisely because understanding why it matters requires the async/await knowledge this course builds up to across Sessions 25-27."
    },
    {
      "prompt": "If GetClockEntry's CancellationToken parameter started being genuinely checked (e.g. with cancellationToken.ThrowIfCancellationRequested()), what real behavior would change for existing callers?",
      "options": [
        "Existing callers passing the default token would see no change at all, since a default CancellationToken is never in a cancelled state -- only callers who explicitly pass and trigger a real token would newly observe OperationCanceledException",
        "Every existing call to GetClockEntry would immediately start throwing",
        "The method's return type would need to change"
      ],
      "correct": 0,
      "explanation": "This is exactly why adding real cancellation support to an existing method is usually a safe, additive change -- default(CancellationToken) callers are unaffected."
    },
    {
      "prompt": "What is the real, functional difference in this codebase between a CancellationToken parameter that exists but is unused, and one that is genuinely checked?",
      "options": [
        "The unused one (GetClockEntry's) has zero effect on the method's behavior no matter what token is passed; the checked one (CancellableEntrySource's) can genuinely stop the operation early via OperationCanceledException",
        "There is no real difference -- both behave identically",
        "The checked one always runs faster"
      ],
      "correct": 0,
      "explanation": "This is the entire teaching point of contrasting the two: a CancellationToken parameter's mere presence in a signature proves nothing about whether cancellation actually works."
    }
  ],
  "reflections": [
    "Why does fixing Program.cs's real bug in Session 28 (rather than Session 26, where it was first identified) feel like the right pedagogical placement now that you understand cancellation?",
    "Where else in this codebase might a parameter exist in a signature without being genuinely used, the way GetClockEntry's CancellationToken is?",
    "What would you need to change about ClockEntryRepository for GetClockEntry's CancellationToken to become genuinely functional?"
  ],
  "whatBreaks": "Reverting Program.cs's await fix reintroduces the real race condition between Clockout's completion and CalculateTotalHours's read of GetEmployeeClockEntries, along with the real CS4014 warning that would then need re-suppressing to keep the build warning-free. Assuming GetClockEntry's CancellationToken parameter provides genuine cancellation support (because the parameter exists) would lead to writing cancellation-dependent code around a method that currently cannot honor it.",
  "summary": "Program.cs's real, long-standing unawaited Clockout(1) call is fixed here, in the session where async/await and cancellation are both fully in scope: adding await removes the race that made CalculateTotalHours's near-zero output an accident rather than a guarantee, and the CS4014 pragma suppression is removed along with the bug it was protecting. GetClockEntry's CancellationToken parameter is confirmed, by reading its real implementation, to be entirely decorative -- a real contrast to CancellableEntrySource's genuinely functioning one."
}
