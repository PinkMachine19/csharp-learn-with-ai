# Distinct Mental Models Redesign Plan

This plan was completed before the second implementation pass. Unless marked otherwise, every item is a **single image**, not a frame counted as a separate model. Each semicolon-separated item records: **exact concept — likely misunderstanding → visual metaphor / central scene and composition**. Items in the same row deliberately use different silhouettes and spatial structures.

| Session and title | Number needed and why | Planned mental models |
|---|---|---|
| 00 — Why Learn C# in 2026? | 4: four different industry-level misconceptions. | Languages have different jobs—not one winner → tool belt with unlike tools; tutorial visibility is not market size → iceberg above/below water; production combines technologies—not one language → orchestra sections around a conductor; system layers collaborate—not compete → cutaway building with specialized floors. |
| 00.5 — Product Overview & Requirements | 4: journey, actors, scope, and vocabulary are separate memories. | Work interval becomes hours—not wages → punch card entering a time calculator; worker and payroll consume different things—not one actor → relay handoff; scope excludes imagined features—not an unlimited backlog → fenced worksite; shared vocabulary stabilizes meaning—not casual synonyms → labeled parts tray. |
| 00.6 — From Requirements to Domain Model | 4: nouns, verbs, relationships, and boundaries require different reading lenses. | Nouns suggest models—not every noun is a class → specimen labels sorted into trays; verbs reveal behavior—not data → control levers moving parts; relationships create meaning—not isolated boxes → constellation joined by meaningful links; responsibilities choose projects—not arbitrary folders → building departments with one-way doors. |
| 01 — Scaffolding the Solution | 4: solution, template output, tests, and Git tracking are distinct mechanics. | Solution lists projects—not source container → binder index pointing to folders; template creates a folder—not magic project state → cookie cutter stamping a folder; test project is ordinary plus a testing job—not exotic → workshop bay with inspection badge; Git tracks files—not empty folders → coat check accepting coats but not empty hangers. |
| 02 — Project References | 4: direction, XML mechanism, layering, and build order differ. | Consumers point to Domain—not both directions → one-way power cords; reference is one XML entry—not hidden magic → manifest line clipped into a project sheet; Domain is stable foundation—not coordinator → foundation supporting replaceable rooms; reference precedes use—not discovered automatically → bridge installed before traffic. |
| 03 — Variables, Expressions, Numeric Types | 4: storage, evaluation, compatibility, and decimal representation differ. | Variable carries a typed value—not merely a label → shaped locker; expression produces a value—not an instruction cloud → calculator chute; type controls fit—not formatting → shape-sorting aperture; decimal models base ten—not generic precision → ten-slot abacus. |
| 03.5 — Choosing Numeric Types | 4: classification, representation, fractions, and tradeoffs each answer a different question. | Choose by purpose—not memorized table → market stall scales; display is not storage—not what digits look like → transparent binary/decimal jars; fractions fit representations differently—not arithmetic failure → gear teeth that do or do not mesh; range/precision/speed trade off—not one best type → three-pan balance. |
| 04 — Control Flow | 4: condition, comparison, pattern mapping, and single-path execution differ. | Condition selects a path—not runs both → railway switch; comparison produces bool—not mutation → balance scale lighting true/false; switch maps shapes to outcomes—not chained decoration → mail sorter slots; one input takes one route—not several arms → marble maze with one exit. |
| 05 — Methods | 4: entry, scope, return, and signature are separate boundaries. | Arguments enter parameters—not shared names → loading dock receiving crates; locals stay inside—not globally available → fenced workbench; return sends one result—not the whole workspace → parcel leaving a hatch; signature advertises permitted use—not implementation → appliance control panel. |
| 06 — Classes and Objects | 4: definition, construction, property access, and instance identity differ. | Class defines shape—not an object itself → architectural blueprint; constructor establishes initial state—not optional cleanup → assembly jig installing required parts; property exposes controlled state—not raw storage → service window; instances share design but not values—not aliases → houses built from one plan. |
| 07 — Initialization and Nullability | 4: optional state, constructor proof, existing invariant, and flow proof differ. | Nullable means value-or-empty—not error → two-position locker; constructor guards birth state—not later hope → hospital discharge checklist; existing constructor already proves members—not defaults required → sealed certificate; null check narrows a branch—not removes null globally → flashlight illuminating one safe path. |
| 08 — Value and Reference Assignment | 4: aliasing, shared mutation, value copying, and mixed object/member semantics differ. | Two references can point to one object—not clone → two remotes/one television; mutation is visible through aliases—not synchronized copies → two windows into one room; values copy independently—not alias → carbon-copy slips separated; object identity and member representation are separate questions → suitcase containing copied tickets. |
| 09 — ref, out, and in | 4: each modifier plus caller handling has its own contract. | out produces a second output—not ordinary return → service window filling caller tray; caller branches on success—not assumes value → green/red claim gate; ref changes caller storage—not local copy → mechanic reaching into caller toolbox; in exposes readonly reference—not immutable object → museum glass viewing port. |
| 10 — Equality | 4: class identity, record value, comparer policy, and struct copying differ. | Class equality defaults to identity—not matching fields → fingerprints; record equality compares components—not references → matching wax seals; string comparison policy is chosen—not universal → adjustable case-sensitive scanner; record struct copies by value—not aliases → duplicated stamped tokens. |
| 11 — Lists and Enumeration | 4: growth, filtering, exposure, and iteration differ. | List grows dynamically—not fixed array → expanding shelf; Where selects without removing—not edits source → sieve over a bin; public list bypasses rules—not harmless convenience → unlocked stockroom door; foreach releases one item at a time—not whole storage → turnstile. |
| 12 — Dictionaries and Sets | 4: keyed lookup, duplicate-key refusal, set uniqueness, and Try-pattern differ. | Dictionary uses a key—not scan → claim-ticket lockers; TryAdd preserves original—not overwrite → occupied parking space rejecting a second car; set keeps one equal item—not counts duplicates → guest-list checkpoint; TryFind reports outcome plus value—not nullable alone → two-part receipt. |
| 13 — Collection Interfaces | 4: boundary, capability ladder, compile-time contract, and real filter are distinct. | Method narrows visible powers—not storage itself → room viewed through a narrow window; interfaces add capabilities—not interchangeable names → graduated tool licenses; compiler permits only declared members—not runtime guesses → security badge reader; completed filter excludes open entries—not arbitrary sample → finish-line gate. |
| 14 — Generic Types and Methods | 4: filling a type, inference, element contract, and closed forms differ. | Generic type receives a type argument—not object erasure → adjustable mold; inference reads arguments—not runtime guessing → caliper selecting a jig setting; IEnumerable names produced element—not storage → labeled conveyor; one definition yields closed forms—not duplicated source → master key cut into several fitted keys. |
| 15 — Generic Constraints | 4: promise, compiler loss, eligibility, and category differ. | Constraint promises capability—not validation → admission badge; removing it removes known operations—not runtime feature → unplugged control panel; qualifying types satisfy contract—not chosen by name → height-test gate; constraint defines a category—not one type → club entrance accepting many valid cards. |
| 16 — Variance | 4: covariance, contravariance, flow rule, and non-applicable API differ. | out safely widens producers—not arbitrary casting → fruit dispenser into general basket; in safely narrows consumers—not input mutation → animal feeder accepting dog food; flow direction determines safety—not keyword mnemonic → loading dock arrows; nongeneric repository has nothing to vary—not invariant failure → fixed-size socket without adapter. |
| 17 — Encapsulation and Composition | 4: bypass, private storage, private setter, and composition differ. | Public list bypasses rules—not just convenient access → broken side door; private storage forces methods—not immutability → guarded storeroom; private setter permits reads but controlled writes—not readonly property → display case/curator key; composition owns collaborators—not inheritance → toolbox containing distinct tools. |
| 18 — Interfaces and Polymorphism | 4: extraction, implementation, declared/runtime type, and substitution seam differ. | Interface extracts existing behavior—not speculative wish list → tracing paper over working controls; implementation retains machinery—not interface storage → machine behind control panel; declared contract narrows view—not changes object → object behind a viewing mask; one implementation can still create a seam—not proof of alternatives → universal socket with one cartridge. |
| 19 — Abstract Classes | 4: incompleteness, shared helper, override contract, and interface choice differ. | Abstract base cannot stand alone—not broken class → unfinished master stencil; descendants share implementation—not copies → family workshop/tool cabinet; override fills required slot—not hides method → missing puzzle-shaped machine part; abstract class and interface solve different relationships—not style preference → family tree versus capability badge. |
| 20 — Constructor Injection | 4: visibility, direction, coordination, and composition root differ. | Constructor exposes dependency—not service locator → visible cable socket; service points to contract—not implementation → arrow toward blueprint; service coordinates collaborators—not owns storage → orchestra conductor; Program wires one shared instance—not hidden container magic → wiring bench. |
| 21 — LINQ Pipelines | 4: composition, filtering, aggregation, and ordering are distinct. | Query chains transformations—not mutates source → factory stations; Where removes from result—not original → inspection sieve; Sum projects then totals—not adds objects → weigh station; ordering states requirement—not incidental insertion → numbered race podium. |
| 22 — Grouping, Joining, Aggregation | 4: grouping, projection, joining, and materialization differ. | GroupBy makes keyed buckets—not sorted list → mail cubbies; Select turns group into summary—not edits group → press forming a report card; Join matches independent sequences—not embeds objects → zipper joining two tapes; ToList freezes report now—not disables LINQ → instant photograph. |
| 23 — Deferred Execution | 4: four execution concepts require four unrelated recall objects. | Deferred query waits—not already-run results → train at an unpulled start lever; enumeration triggers work—not declaration → turnstile releasing items; ToList snapshots now—not permanently live → photograph beside changing scene; IQueryable delegates to provider—not in-memory iteration → restaurant order translated in kitchen. |
| 24 — Validation and Exceptions | 4: normal rejection, invalid state, contract signal, and tests differ. | Expected rejection stays normal—not exceptional → venue turnstile; invalid state throws—not false → emergency road barrier; return type communicates meaning—not syntax choice → labeled signal flags; tests preserve separate contracts—not catch everything → two sealed evidence bags. |
| 25 — Exception Propagation and Disposal | 4: propagation, termination, guaranteed cleanup, and resource contract differ. | Exception climbs until caught—not copied at every layer → flare rising through floors; unhandled failure stops process—not generic recovery → severed power switch; using cleans up on both paths—not only success → janitor closing gate after evacuation; IDisposable releases external resources—not memory generally → rented-tool return tag. |
| 26 — Tasks and Awaitable Work | 4: suspension, thread independence, completed task, and dropped task differ. | await suspends work—not blocks a thread → restaurant pickup pager; async promises no thread—not background worker → reservation ticket without assigned chair; completed Task can hold known result—not fake delay → already-filled claim envelope; unawaited Task escapes observation—not guaranteed completion → untethered balloon. |
| 27 — Sequential and Concurrent Async | 4: sequence, fan-out, measured concurrency, and non-batch case differ. | Sequential awaits one at a time—not slow concurrency → single checkout lane; concurrent starts all before waiting—not parallel guarantee → several service windows; counter proves overlap—not timing guess → occupancy meter; single-item Clockout has no batch decision—not missed optimization → one package/no sorting fan-out. |
| 28 — Cancellation and Async Exceptions | 4: race, ordered fix, decorative token, and observed token differ. | Missing await races—not nearly correct order → runners leaving before baton; await establishes order—not merely syntax → relay handoff; unused token does nothing—not automatic cancellation → emergency cord disconnected; observed token interrupts wait—not kills thread → stop button wired to conveyor. |
| 29 — Unit Tests and AAA | 4: Arrange, Act, Assert, and unused dependency each need a separate lab-bench cue. | Arrange builds conditions—not executes subject → specimen preparation bench; Act is one operation—not setup continuation → single lever pull; Assert checks outcome—not internals → calibrated gauge; unconfigured mock proves no call—not incomplete test → untouched tripwire. |
| 30 — Test Doubles and DI Lifetimes | 4: double forms, substitution, real lifetimes, and scoped lifetime differ. | Moq and hand fake are two costumes—not different contracts → two stunt doubles; interface enables substitution—not DI itself → universal role badge; singleton/transient differ in reuse—not class kind → reusable versus disposable tickets; scoped is shared inside boundary—not halfway singleton → hotel room key valid for one stay. |
| 31 — EF Core Tracking | 4: persistence gap, framework inheritance, entity shape, and tracking state differ. | Memory disappears on restart—not persistence → chalkboard wiped overnight; DbContext inheritance is framework requirement—not domain design → railcar locked to standard gauge; entity mirrors storage—not domain behavior → shipping manifest versus working machine; Add tracks before save—not writes immediately → warehouse clipboard marked pending. |
| 32 — EF Query Execution | 4: expression tree, translation, execution, and process boundary differ. | IQueryable records a recipe—not loops now → untranslated kitchen ticket; provider translates to SQL—not sends C# → interpreter booth; ToListAsync executes—not just changes type → order bell releasing plates; database query crosses process boundary—not IEnumerable memory loop → border checkpoint. |
| 33 — ASP.NET Core Requests | 4: missing endpoint, action, boundary parameters, and request route differ. | Mapping infrastructure without controller maps nothing—not automatic API → empty train platform; action supplies endpoint—not service method alone → staffed ticket booth; HTTP inputs differ from domain call—not same boundary → customs declaration; request follows route to service—not direct jump → addressed parcel route. |
| 34 — API Failures and Logging | 4: registration bug, latency of discovery, proof, and failure category differ. | Missing DI registration is latent wiring fault—not business error → unplugged cable behind wall; unused path hides bug—not validates setup → unopened trapdoor; paired tests prove failure/fix—not happy path only → before/after circuit tester; configuration failure differs from rule rejection—not one error bucket → airport lanes for ticket, conflict, system. |
| 35 — End-to-End Verification | 4: stages, validation guard, result branch, and integration proof differ. | Request crosses explicit stages—not magic pipeline → sealed parcel checkpoints; validation blocks writes—not cleans later → dam before reservoir; result selects path—not exception for all outcomes → railway signal; integration test verifies real boundary—not isolated mock → inspector following package end to end. |
| 01B — VS Code Fluency | 4: navigation, layout, debug target, and recovery are independent editor skills. | Navigate by intent—not folder hunting → compass/search beacon; manipulate views without changing files—not project mutation → movable stage panels; F5 follows selected target—not visible file → train departure board; Command Palette recovers forgotten actions—not shortcut memorization → labeled master control panel. |
| 03B — Formatting Output | 4: history, interpolation, representation, and equivalent syntax differ. | Syntax accumulated—not replacements → geological text strata; interpolation places values inline—not postprocessing → label printer inserting tiles; format specifier changes display—not number → tinted viewing lens; several syntaxes can yield same text—not same readability → different roads reaching one sign. |
| 04C — Choosing Branch Syntax | 4: Boolean questions, statement cases, value mapping, and judgment differ. | if asks questions—not value table → detective checklist; switch statement executes a case—not returns inherently → rotary selector operating tools; switch expression produces value—not statement block → vending machine output; problem shape chooses tool—not modern always wins → carpenter choosing saw, drill, plane. |
| 05B — Properties | 4: controlled access, accessor behavior, storage strategy, and stable contract differ. | Property controls access—not public field → guarded mailbox; accessors execute like methods—not passive slot → two service windows; backing versus auto storage differs internally—not publicly → visible cabinet beside concealed drawer; public surface can stay same while storage changes—not caller rewrite → unchanged house facade with remodeled room. |
| 19B — Access Modifiers | 4: rule protection, assembly, inheritance, and sealing differ. | Public operation/private rule prevents bypass—not inconvenience → bank teller/vault; internal ends at assembly—not solution → factory employee gate; protected serves descendants—not callers → family-only staircase; public and sealed answer access versus inheritance—not opposites → open museum with locked expansion joint. |
| 24B — Outcome Contracts | 4: boundary translation, normal/exceptional distinction, return shape, and tests differ. | One event changes representation across layers—not same message everywhere → multilingual relay; expected versus exceptional uses different lanes—not preference → hospital triage; smallest useful return avoids excess detail—not bool always → measuring cup sizes; tests preserve decisions while comments expire—not equal artifacts → pencil note versus notarized contract. |
| R1 — C# Evolution | 4: chronology, accumulation, toolchain layers, and availability differ. | Versions form one evolving language—not separate dialect islands → growing tree rings; features layer on foundations—not replace core → archaeological strata; language/SDK/runtime/framework are distinct—not synonyms → four-machine workshop stack; project version gates syntax—not conceptual invalidity → amusement ride height marker. |
| R2 — Generics, Lambdas, LINQ | 4: type preservation, constraints, behavior values, and transformation differ. | Generic preserves concrete type—not object box → shape-specific reusable mold; constraint requires capability—not identity → tool-license checkpoint; lambda packages behavior—not executes immediately → recipe card; LINQ composes operators—not mutates source → water treatment stations. |
| R3 — async/await | 4: suspension, Task state, cancellation, and concurrency/parallelism differ. | before/after await resumes one method—not restart → bookmark in a paused play; Task carries eventual state—not thread → claim ticket; cancellation is cooperative—not forced termination → stop request passed down relay; concurrency differs from parallel CPU work—not synonyms → cook juggling timers versus two cooks chopping. |
| R4 — Patterns and Nullable References | 4: shape matching, order, annotation, and flow evidence differ. | Pattern matches structure—not constants only → airport luggage shape scanner; specific arms precede catch-all—not arbitrary order → nesting sieves; nullable annotation declares possibility—not runtime wrapper → optional seat reservation; successful check refines local knowledge—not object globally → fog lifting on one trail. |
| R5 — Modern Construction | 4: records, init/required, primary constructors, and collection expressions differ. | Record emphasizes value identity—not immutable automatically → stamped identity card; init/required controls construction window—not content validity → wet concrete gate before curing; primary constructor parameters are in scope—not automatic properties → parts hopper feeding whole machine; collection expression describes elements with target context—not always List → basket accepting a specified container shape. |

## Implementation rules

- Preserve the course palette, typography, card layout, and SVG accessibility conventions.
- Render every planned item as its own scene. Do not derive several cards by highlighting different parts of one full-session scene.
- Use a progression only when the plan explicitly calls for a before/during/after transformation; no planned row currently counts progression frames as separate mental models.
- Re-audit all 50 pages after generation using the teaching, recall, text-removal, duplication, and necessity tests.
